if {valuedeterminatio1) rejected;
then use(roots) {

sq^ = input * itself(2) = answer(def eq1)
cu^ = input * itself(3) = answer(>)
^{def} = input(^input) = answer

. = sig(decimal = input.input)
>( {more than value(input) } )
<( {less than value(input) } )

() = (first.operation{execute})
~ = (slope.operation between .value(input to= input.value)

a-b(can be(utilize (as=) variable)
def(input) from{var} (numeral;replace)
#(signifies{null})

     }
     
